<script setup lang="uts">
  import { hydrateUserStore } from '@/store/user.uts'

  /**
   * 初始化应用基础状态。
   */
  const bootstrapApp = () => {
    hydrateUserStore()
  }

  onLaunch(() => {
    bootstrapApp()
  })
</script>

<style>
  @import "./common/uni.css";

  page {
    background-color: #f5f7fb;
    color: #1f2937;
  }
</style>
